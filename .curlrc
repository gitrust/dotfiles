# Default curl configfile
# When curl is invoked, it always (unless -q is used) 
# checks for a default config file and uses it if found

# When following a redirect, automatically set the previous URL as referer.
referer = ";auto"
